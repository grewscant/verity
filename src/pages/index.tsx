import type { NextPage } from "next";
import { Navbar } from "../components/Navbar";

const Home: NextPage = () => {
  return (
    <div>
      <Navbar title="V" />
    </div>
  );
};

export default Home;
